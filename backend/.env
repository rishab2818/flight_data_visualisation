# Database (psycopg2 driver)
DATABASE_URL=postgresql+psycopg2://flightuser:1234@127.0.0.1:5432/flightdb

# Connection pool
DB_POOL_SIZE=10
DB_MAX_OVERFLOW=20
DB_POOL_RECYCLE=1800

# Optional Redis/Celery (leave empty to keep threading)
REDIS_URL=

# JWT
JWT_SECRET=dev_secret_change_me
JWT_ALG=HS256
JWT_EXPIRE_MINUTES=4320

# Data dir
# Keep your current structure (backend/core/config.py already builds paths relative to backend/)
